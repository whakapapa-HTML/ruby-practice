// post.idはrelationshipコントローラーに渡す
// ボタンクリック => コントローラー　=> jsファイルの順に変数の情報が渡される
- if post.user.is_followed_by?(current_user)
  = link_to user_relationships_path(user_id: post.user.id, post_id: post.id), class: "position-relative ml-4", remote: true, method: :delete do
    %i.fas.fa-user.following_color.text-dark
- else
  = link_to user_relationships_path(user_id: post.user.id, post_id: post.id), class: "position-relative ml-4", remote: true, method: :post do
    %i.far.fa-user.following_color